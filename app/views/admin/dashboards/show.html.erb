<div class="col-xs-12 categories text-center">
<% Category.all.each do |t| %>

<div class="col-sm-3 cat">
  <h3>New <%= link_to "#{t.name}", new_admin_category_post_path(t) %> post</h3>
  <p><%= "#{t.name}" %></p> <br /></div>

<% end %>
</div>
